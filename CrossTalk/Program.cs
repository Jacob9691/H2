
Reciter.ReciteAllTheWords();

Console.WriteLine("Reciting Completed (press any key to close App)...");
Console.ReadLine();

/*
 Task notes:
    3: The reason why it prints the last line. Is because the code moves on after starting the tasks, 
        because the code does not wait for the tasks to finish. Therefor it gets to the print last line,
        way before it gets to printing the tasks out.
 */
